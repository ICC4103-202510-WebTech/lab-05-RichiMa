<h1>My Inbox</h1>

<ul>
  <% @chats.each do |chat| %>
    <li>
      Chat with <%= link_to chat.other_participant(current_user).email, chat_path(chat) %>
    </li>
  <% end %>
</ul>

<%= link_to 'New Chat', new_chat_path %>