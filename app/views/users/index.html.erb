<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to "#{user.first_name} #{user.last_name} #{user.email} #{user.created_at} #{user.updated_at}", user_path(user) %>
    </li>
  <% end %>
</ul>